import React from 'react';
import './notFound.scss'

const NotFound = () => {
    return (
        <section className='notFound'>
            notFound
        </section>
    );
};

export default NotFound;